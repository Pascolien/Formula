//these variables permit to customize components in TEEXMA
var iTxFormWdowWidth = 1130, // the form window width
    iTxFormWdowHeight = 650, // the form window height
    iTxFormFieldWidth = 527, // a field corresponding to a label + a data in a form
    iTxFormComponentsWidth = 390, // the data's width
    iTxFormComponentsHeight = 100, // the data's height
    iTxFormComponentsMaxHeight = 200, // the data's height
    iTxFormTreesHeight = 74, // the tree's height in a form
    iTxFormGridsHeight = 155, // the grid's height in a form
    iTxFormDatepickerWidth = 125, // the combo's width in a form
    iTxFormPublisherWidth = 339, // the combo's width in a form
    iTxFormAreasMaxHeight = 4, // the max size in height of textareas, the textarea change his size in height when we made a back to line
    iTxFormCombosLnkWidth = 364, // the combo's width in a form
    iTxFormCombosLnkMaxExpandedHeight = 150, // the combo's height in a form when we expand the options
    iTxTinyMceWidth = 390, // the tinyMCE width in a form
    iTxTinyMceHeight = 100; //the tinyMCE height in a form



